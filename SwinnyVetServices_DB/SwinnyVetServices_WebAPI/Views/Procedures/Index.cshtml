@model IEnumerable<SwinnyVetServices_WebAPI.Models.Procedure>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.procedureDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.procedurePrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.procedureDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.procedurePrice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.procedureId }) |
            @Html.ActionLink("Details", "Details", new { id=item.procedureId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.procedureId })
        </td>
    </tr>
}

</table>
